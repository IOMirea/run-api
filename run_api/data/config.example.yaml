config-version: 0

postgresql:
  host: "localhost"
  port: "5432"
  user: "iomirea"
  database: "run"
redis-rpc:
  host: "localhost"
  port: "6379"
sentry:
  dsn: ""
manager:
  docker-hub-webhook-path: "docker-hub-webhook"
